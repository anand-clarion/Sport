<h2>New Comment</h2>
<%= form_for [@commentable, @comment] do |f| %>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content, :rows => 5 %>
  </p>
  <p>
    <%= submit_tag "Add comment" %>
  </p>
<% end %>